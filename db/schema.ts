// import { text, integer, sqliteTable } from "drizzle-orm/node-postgres";

// export const users = sqliteTable('nouns', {
//     id: integer('id', { mode: 'number' }).primaryKey({ autoIncrement: true }),
//     name: text('name'),
//     isProper: integer('isProper', { mode: "boolean"}).default(false),
//     isAgent: integer('isAgent', { mode: "boolean"}).default(false),
//     season: text('season'),
//     updatedAt: text("updatedAt").default(sql`(CURRENT_TIMESTAMP)`),
// });
